                //string msg = serialPort.ReadLine();
                //try
                //{
                //    int currentTemp = Int32.Parse(msg.Substring(8));
                //    //string url = "http://dweet.io/dweet/for/627929fa-c0a4-4f9d-a2b7-f958e1161d96?temp=" + msg.Substring(8);
                //    if (previousTemp != currentTemp)
                //    {
                //        Console.WriteLine("Starting...");

                //        HttpWebRequest request = (HttpWebRequest)HttpWebRequest.Create("http://dweet.io/dweet/for/627929fa-c0a4-4f9d-a2b7-f958e1161d96");

                //        request.AllowWriteStreamBuffering = true;
                //        request.Method = "POST";
                //        string post = "temp=" + msg.Substring(8);
                //        byte[] byteArray = Encoding.UTF8.GetBytes(post);
                //        request.ContentLength = byteArray.Length;
                //        request.ContentType = "application/x-www-form-urlencoded";

                //        Stream dataStream = request.GetRequestStream();
                //        // Write the data to the request stream.
                //        dataStream.Write(byteArray, 0, byteArray.Length);
                //        // Close the Stream object.
                //        dataStream.Close();
                //        WebResponse response = request.GetResponse();
                //        // Display the status.
                //        Console.WriteLine(((HttpWebResponse)response).StatusDescription);
                //        // Get the stream containing content returned by the server.
                //        dataStream = response.GetResponseStream();
                //        // Open the stream using a StreamReader for easy access.
                //        StreamReader reader = new StreamReader(dataStream);
                //        // Read the content.
                //        string responseFromServer = reader.ReadToEnd();
                //        // Display the content.
                //        Console.WriteLine(responseFromServer);
                //        // Clean up the streams.
                //        reader.Close();
                //        dataStream.Close();
                //        response.Close();

                //        Console.WriteLine("Sent: " + msg.Substring(8));
                //    }
                    
                //    previousTemp = currentTemp;
                //}
                //catch (Exception e)
                //{
                //    Console.WriteLine(e.Message);
                //    Console.WriteLine(e.StackTrace);
                //}